# Frontend API base path. Keep /api in development to use Vite proxy.
VITE_API_BASE_URL=/api

# Lambda Function URL target for Vite dev proxy.
VITE_BACKEND_URL=https://your-lambda-id.lambda-url.us-east-1.on.aws/
