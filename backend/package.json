{
  "name": "setback-ai-backend",
  "version": "1.0.0",
  "private": true,
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test test/**/*.test.js",
    "tf:init": "tofu -chdir=../infrastructure init",
    "tf:apply": "tofu -chdir=../infrastructure apply"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.888.0",
    "@aws-sdk/lib-dynamodb": "^3.888.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "typescript": "^5.9.2"
  }
}
